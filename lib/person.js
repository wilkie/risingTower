/* Person object handles each and every person that exists within your Tower.
 */
var initPerson = function(RisingTower) {
  var Person = RisingTower.Person = function() {
  };

  Person.preload = function() {
    // Load the person graphics.
  };
};
